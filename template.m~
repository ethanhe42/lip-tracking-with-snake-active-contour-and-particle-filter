clear all

% dir_name='liptracking2';
% start_frame=1302;
directory_name='liptracking4';
start_frame=68;
dir=['./',directory_name,'/',directory_name,'_',num2str(start_frame,'%05d'),'.jpg'];
    
img=imread(dir);
imshow(img)
clear dir_name start_frame img
[x,y]=ginput(20);
save(['template',directory_name(length(directory_name)),'.mat']);
